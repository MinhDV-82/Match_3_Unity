using UnityEngine;

public class ColorBoomCandy : BaseCandy
{
    public override void Init(int x, int y, CandyColor dotColor, CandyType candyType = CandyType.Regualar)
    {
        base.Init(x, y, dotColor, candyType);
    }
    
}